{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write . --ignore-path .gitignore",
    "build": "wsrun -ems build",
    "test": "wsrun -ems test",
    "redux": "yarn workspace @web-ext/redux"
  },
  "engines": {
    "node": ">=12",
    "yarn": "1"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,md,html,css,graphql}": "prettier --write"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "post-commit": "git update-index --again"
    }
  },
  "resolutions": {
    "typescript": "4.0.3"
  },
  "devDependencies": {
    "husky": "^4.3.0",
    "lint-staged": "^10.4.0",
    "prettier": "^2.1.2",
    "typescript": "^4.0.3",
    "wsrun": "^5.2.4"
  }
}
